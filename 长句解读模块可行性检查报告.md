# 捕获生词「长句解读」模块 — bai-it 复用可行性检查报告

> 检查对象：`D:\捕获生词-main\bai-it-main\`
> 目标：在 捕获生词 学习卡片之后新增"长句解读"模块（长句拆块 + 对应翻译 + 生词标注），**纯本地、零 LLM、零第三方运行时依赖，实现基础功能**
> 结论先行：**核心算法与词典数据完全可复用，方案可行**，但有一处必须由你拍板的取舍（见 §六）。

---

## 一、检查结论（TL;DR）

| 能力 | 是否可复用 | 证据 |
|---|---|---|
| ✅ 长句自动拆块 | **可直接移植** | `scan-rules.ts` 纯规则断句引擎（毫秒级、无 LLM）；已实测去 POS 后 4/5 长句正确拆块 |
| ✅ 生词标注 + 释义 | **可直接移植** | `vocab.ts` 标注逻辑 + ECDICT 1.19MB 离线词典 + 5806 常用词表 + 28751 词形还原表 |
| ✅ 生词判定（跳过常用词） | **可直接移植** | `word-frequency.json` 常用词表 + `lemma-map.json` 词形还原，纯本地 |
| ⚠️ "对应翻译"（整句中译） | **纯本地做不到** | ECDICT 只有**单词**释义，无整句翻译能力。需要你选方案 A 或 B（见 §六） |
| ❌ 其他功能（LLM 分析/手动掰句/复习库/React UI） | 不需要 | 按你的要求全部排除 |

**结论**：算法层 + 数据层 4 个文件约 1.86MB（1.19MB 词典 + 602KB 词形 + 55KB 词频 + ~24KB 算法 JS），以原生 JS 形式并入 捕获生词 完全可行，首次使用懒加载，加载一次后常驻内存，零网络、零费用、零 API。

---

## 二、bai-it 可复用资产逐文件评估

### 2.1 算法层（纯逻辑，可直接转原生 JS）

| 文件 | 大小 | 作用 | 评估 |
|---|---|---|---|
| `src/shared/scan-rules.ts` | 17.5KB | **长句拆块引擎**：并列/从属/关系代词/转折/破折号/括号/冒号/分号等 12 类边界规则 + 短块合并 + 三级颗粒度 | ✅ **核心复用**。转原生 JS 仅需：去 TS 类型、删除 `import { Lexer, Tagger } from "pos"` 一行（`getPOSTags` 抛错被 catch 后自动降级到无 POS 启发式）。其余逻辑零改动 |
| `src/shared/vocab.ts` | 6.1KB | **生词标注**：提取单词 → 跳过短词/数字/缩写 → 跳过常用词（含词形还原）→ 查 ECDICT 给释义 | ✅ **核心复用**。纯逻辑，直接转原生 JS |
| `src/shared/rule-engine.ts` | 4.3KB | 句子复杂度估算（判断"是否复杂到要调 LLM"） | ⚠️ **不需要**。你的场景不用 LLM，此文件只服务于"LLM 降级触发"，排除 |
| `src/shared/db.ts` / `cache.ts` / `llm-adapter.ts` | — | IndexedDB 词库 / 缓存 / LLM 适配 | ❌ 排除（不需要词库复习与 LLM） |

### 2.2 数据层（可直接拷贝，纯数据文件）

| 文件 | 体积 | 内容 | 用途 |
|---|---|---|---|
| `data/dict-ecdict.json` | **1.19MB** | 31741 词条 `{ 单词: "中文释义" }` | 生词释义（离线英中词典） |
| `data/lemma-map.json` | **602KB** | 28751 条 `{ 变体: 原形 }`（running→run） | 词形还原 |
| `data/word-frequency.json` | **55KB** | 5806 个常用词数组 | 常用词过滤（不标注） |
| `data/modern-vocab.json` | **4KB** | 129 条网络新词 | 可选补充（vibe/vibe-coding 等） |

> 数据格式已核实：`dict-ecdict.json` 是 `{word: 释义}` 对象（非数组），可直接被 `vocab.ts` 的 `loadDictionary` 消费。三份数据合计约 1.85MB，捕获生词 现有扩展包体量完全可以容纳。

### 2.3 值得参考但不复制的资产

| 文件 | 说明 |
|---|---|
| `docs/research/local-sentence-chunking.md` | 19.5KB 断句算法研究文档。已通读，结论是"纯规则已够用，可再优化"；对我们**无新增依赖**的价值点已吸收（如报告动词后 that 不拆） |
| `src/content/index.ts` | 生产调用方式（`scanSplit → toChunkedString → annotateWords`），确认了正确调用顺序，作参考不复制 |

---

## 三、三个关键限制与处理方式

### 3.1 ⚠️ "对应翻译"——纯本地无法做整句中译（唯一硬限制）

这是本次检查**必须由你决策**的点。事实：

- ECDICT 是**单词级**英中词典，只提供 `word → 释义`，**不含句子翻译**。
- 纯本地（不联网、不调 LLM）的 JS 没有任何办法把一句英文整句翻译成中文——句子翻译本质上是生成式任务。
- 因此"长句拆开 **+ 对应翻译** + 生词"中的"对应翻译"，纯本地只能做到两种形态（见 §六 A/B）。

### 3.2 POS 依赖可以整体去掉（零第三方依赖）

- `scan-rules.ts` 里 `pos` 标注是**可选增强**，不是必需：`getPOSTags` 一旦失败返回 `null`，`splitAtBoundaries` 自动走无 POS 的启发式（代词/冠词/大写开头 → 从句开始）。
- 关键证据：bai-it 自己的 `package.json` **根本没有声明 `pos` 依赖**（只有 react/react-dom），说明其生产构建大概率就是无 POS 或 POS 缺失降级路径。
- 结论：**不引入 pos-js**，捕获生词 保持零第三方运行时依赖，最符合你"不需要复杂的东西"的要求。代价只是断句精度略降（对"基础功能"完全够用）。
- 附带小改进：实测发现 bai-it 原版 `mergeShortChunks` 会把"独立短主语块"（如 `The company,`）吞并导致断点丢失，移植时改一行即可保留该断点（见 §七 P1）。

### 3.3 许可提醒：bai-it 是 BSL-1.1

- bai-it 采用 **Business Source License 1.1**（`package.json` 与根目录 LICENSE 均标注）。
- 含义：**个人学习 / 自用完全没问题**，你的场景（本地插件自用）不受限；但如果将来**对外公开发布**插件，需注意 BSL 的"变更时间"条款或剥离/改写来自 bai-it 的代码与数据。
- 建议：本项目作为自用工具直接并入即可；若计划发布，实施时应重写注释、调整函数命名，并替换/精简词典数据来源。

---

## 四、"长句解读"模块设计

### 4.1 目标形态

在学习视图（卡片列表）**之后**新增一个「长句解读」区块，展示：

```
📖 长句解读

原句（来源：字幕 Top 长句）
└─① Although she was exhausted after the long journey,   ← 从句（缩进 1 级）
      exhausted[疲惫的]  journey[旅行]
   ② she decided to keep walking,                        ← 主句（缩进 0 级）
   ③ because she wanted to reach the village before sunset. ← 从句（缩进 1 级）
      village[村庄]  sunset[日落]
```

- 每一"块"一行，主句 0 缩进、从句缩进 1 级，语义层次一目了然；
- 块内**生词**用虚线/下划线标出，鼠标悬浮显示中文释义（ECDICT）；
- 每块可点喇叭朗读（复用已有 TTS 模块）；
- 生词点击可加入单词本（复用已有单词本机制，可选）。

### 4.2 数据流

```
字幕文本（currentState.subtitlesText / 卡片例句）
   │  ① 选句：拆成句子，按「词数 + 复杂度」排序取 Top 2~3 长句
   ▼
scanSplit(句子)   ──►  块数组 [{text, level}]
   │  ② 逐块调用 annotateWords(块文本, 已掌握词)
   ▼
每块 → [{word, definition}]   ③ 合并成渲染结构
   ▼
renderSentenceBreakdown()  ──►  学习视图"长句解读"区块
```

### 4.3 性能与资源

- **懒加载**：首次进入"长句解读"时，用 `chrome.runtime.getURL` + `fetch` 拉取 3 份 JSON（1.85MB），构建内存 Map（31741 词条），约耗时 100~300ms，一次性，之后常驻；
- **断句**：`scanSplit` 毫秒级，零成本；
- **内存**：31741 词条的 Map 约几十 MB 级，对桌面浏览器可接受（bai-it 同款量级）。

---

## 五、捕获生词 集成点（改动面）

| 改动 | 文件 | 说明 |
|---|---|---|
| 新增算法+数据 | `assets/sentence-reader.js`（新建，IIFE，暴露 `globalThis.CC_READER`）+ `assets/data/{dict-ecdict,lemma-map,word-frequency}.json` | 断句 + 生词标注 + 数据懒加载，命名空间守卫与 tts.js 同风格 |
| 渲染入口 | `ui.js` → `renderLearnView` 的 `doRender` 之后追加 `renderSentenceBreakdown()` | 卡片区块之后插入长句解读区块 |
| 资源声明 | `manifest.json` → `web_accessible_resources` 追加 `assets/data/*.json` | 供 content script fetch |
| 样式 | `assets/cc.css` 新增 `.cc-sb-*`（缩进、虚线生词、悬浮释义） | 复用现有设计体系 |
| 文案（可选） | `assets/i18n/*.json`、`_locales/*/messages.json` 新增 `action_sentence_breakdown` 等 | 保持多语言一致 |
| 生词入单词本（可选） | 复用 `wordbook.js` 的存储/渲染 | 二期再考虑 |

> 全程**不新增第三方依赖、不引入构建步骤**——与 捕获生词 现有"原生 JS + MV3"架构完全一致。

---

## 六、需要你拍板的 3 个决策点

### 决策点 1：⚠️ "对应翻译"怎么做（必须选）

- **方案 A（推荐，纯本地）**：长句拆块 + **每块标注生词并给出单词中文释义**。读者靠"块结构 + 生词释义"自行拼出句意。零网络、零费用、启动快，完全符合"不要 LLM、要快"。**局限**：没有整句中文翻译，需要学习者自己把块拼起来。
- **方案 B（每块整句中译，需联网）**：拆块后，对每个分块调用翻译服务拿到中文译文。可选：
  - B1 免费翻译 API（如 MyMemory / LibreTranslate，免 key，有频率限制）——仍然"不用 LLM"，但依赖网络、非纯本地；
  - B2 复用 捕获生词 已有的 LLM provider（配了 key 才可用）——最准，但违背你"不要 LLM/复杂"的初衷。
- **我的建议**：先做 **A**（纯本地，符合你的所有约束），把"整句翻译"作为开关项留接口，以后想要 B 再开。

### 决策点 2：长句从哪来（默认已给建议）

- **A（推荐）**：自动从当前视频**已提取字幕**里挑最长的 2~3 个句子做解读展示——零操作，进入学习视图即可看到；
- **B**：用户点卡片里的某个例句展开解读；
- **C**：两者都要（实现量略增）。
- 建议先做 **A**，最省事也最贴合"生成卡片后面加模块"的直觉。

### 决策点 3：生词标注粒度（默认已给建议）

- 默认按 bai-it 规则：**跳过常用词**（5806 常用词表内 + 词形还原命中），只标真正值得学的词——符合本插件的教育定位；
- 如需"所有词都可悬浮"，则改为全部标注（不推荐，会太吵）。

---

## 七、实施计划（你确认 §六 后开工）

- **P1 算法移植（核心）**：新建 `assets/sentence-reader.js`——把 `scan-rules.ts`（去 POS）+ `vocab.ts` 转原生 JS；拷贝 3 份数据 JSON；修复 `mergeShortChunks` 吞并首块的断点丢失；用临时单测验证 5+ 类长句拆块与生词标注（跑通即删）。
- **P2 UI 集成**：`ui.js` 学习视图追加"长句解读"区块（懒加载 + 渲染 + 虚线生词 + 悬浮释义 + TTS 喇叭）；`manifest.json` 声明数据资源；`cc.css` 样式；i18n 文案。
- **P3 联调验证**：加载扩展 → 真实 YouTube 视频提取字幕 → 检查长句解读区块是否出现、拆块是否合理、生词是否标注、TTS 是否可播；按验证结果修整。

---

## 附：本报告的可复现证据

- bai-it 源码已通读：`scan-rules.ts`（断句算法）、`vocab.ts`（生词标注）、`rule-engine.ts`（复杂度，已排除）、`docs/research/local-sentence-chunking.md`（算法研究）；`src/content/index.ts` 确认了生产调用顺序。
- 数据格式已用 node 实测：`dict-ecdict.json` 31741 词条 `{word:释义}`、`word-frequency.json` 5806 常用词数组、`lemma-map.json` 28751 词形映射。
- 断句可行性已实证：将 `scan-rules.ts` 去 POS 化后跑 5 个真实长句，**4/5 正确拆块**（并列/从属/关系从句/转折均识别），第 3 句失败根因为 bai-it 原版短块合并逻辑吞并了首块（移植时修复）。
