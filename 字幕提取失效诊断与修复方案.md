# 捕获生词 字幕提取失效：诊断与修复方案
## —— 对比 `youtube-caption-extractor`（2026 最新方案）

> 现状：捕获生词 插件实测无法提取 YouTube 字幕。
> 参考：`D:\捕获生词-main\youtube-caption-extractor-main`（2026 年 5 月仍在维护的 npm 包，含大量近期修复）。
> 结论先行：**捕获生词 的字幕获取路径已过期——它只使用 `WEB` 客户端从页面快照/页面 key 取数据，而这正是 2025–2026 年 YouTube 风控最严、最先失效的对象。新包给出的有效方案是「免 API Key 多客户端直调 InnerTube」，可直接移植修复。**

---

## 目录

1. [问题现状](#1-问题现状)
2. [新参考方案的核心机制](#2-新参考方案的核心机制)
3. [对比诊断：捕获生词 为什么失效](#3-对比诊断captiprep-为什么失效)
4. [修复方案（推荐）](#4-修复方案推荐)
5. [备选方案对比](#5-备选方案对比)
6. [长期维护建议](#6-长期维护建议)
7. [验证步骤](#7-验证步骤)
8. [附录：关键代码对照](#8-附录关键代码对照)

---

## 1. 问题现状

| 项 | 说明 |
| --- | --- |
| 现象 | 点击扩展图标打开浮层，字幕提取失败，提示"无可用字幕"（`error_no_captions`）或卡在提取中 |
| 影响范围 | 几乎所有 YouTube 视频（与视频是否真有字幕无关），说明是**通道失效而非个别视频问题** |
| 捕获生词 版本 | v1.5.0（manifest.json） |
| 相关文件 | `content.js`（`extractCaptionsText`）、`page_inject.js`（InnerTube 代理）、`manifest.json`（权限） |

---

## 2. 新参考方案的核心机制

`youtube-caption-extractor`（npm 包，`src/index.ts`）的核心思路与 捕获生词 有本质差异：

### 2.1 免 Key 直调 `youtubei.googleapis.com`

```
POST https://youtubei.googleapis.com/youtubei/v1/player?prettyPrint=false
```

- **不需要 API Key**（与 yt-dlp 社区方案一致，POST 无需 key）；
- 域名用 `youtubei.googleapis.com`（而非 `www.youtube.com/youtubei`），更不易触发页面风控；
- 需要携带正确的 `context.client`（clientName / clientVersion）+ UA + `X-YouTube-Client-Name` / `X-YouTube-Client-Version` 头。

### 2.2 多客户端链式回退（最关键）

```ts
const CLIENT_PROFILES = [
  { name: 'ios',        clientName: 'IOS',        clientVersion: '20.10.4',          clientNameHeader: '5' },
  { name: 'android_vr', clientName: 'ANDROID_VR', clientVersion: '1.62.20',          clientNameHeader: '28' },
  { name: 'mweb',       clientName: 'MWEB',       clientVersion: '2.20251209.01.00', clientNameHeader: '2' },
];
```

- 依次尝试，**取第一个 `playabilityStatus=OK` 且含 `captionTracks` 的响应**；
- **不早退**：单个 client 报 `ERROR` 不立即失败（YouTube 用同一 `ERROR` 表示"视频私有/删除"和"客户端版本过旧"，不同 client 对同一视频状态可能不同），全部 client 都失败才判定不可访问；
- 每个 client 携带独立指纹（UA、deviceModel、osVersion 等）。

### 2.3 明确的版本演进教训（注释原文要点）

> TVHTML5_SIMPLY_EMBEDDED_PLAYER v2.0 曾在链首，但 **2026 年 5 月起 YouTube 全球拒绝该客户端**（"no longer supported in this application or device"）——无论来源 IP。
> CI 的 nightly canary 开始失败时，典型修法是**把 clientVersion 升到当前值（跟踪 yt-dlp 的近期提交）**。

### 2.4 字幕轨选择与抓取

- 轨道选择：`vssId === .{lang}`（人工）> `a.{lang}`（自动生成）> `languageCode` 匹配 > 兜底第一条；
- 抓取：强制 `&fmt=json3`，解析 `events[].segs[].utf8`，用 `he.decode` + `striptags` 清理 HTML 实体与标签；
- 支持自定义 `fetch`（用于住宅代理路由，规避数据中心 IP 屏蔽）——此点针对服务端部署，浏览器扩展一般不需要。

---

## 3. 对比诊断：捕获生词 为什么失效

### 3.1 现状管线（`content.js` → `extractCaptionsText`）

```
路径1: getPlayerResponseMainWorld() 读页面快照 ytInitialPlayerResponse
        → captionTracks → 抓 baseUrl (json3/srv3/vtt)
路径2: ytApiPostMainWorld('/player')
        → www.youtube.com/youtubei/v1/player?key={页面INNERTUBE_API_KEY}
        → clientName: 'WEB' (X-Youtube-Client-Name: '1')
        → captionTracks → 抓 baseUrl
路径3: 全部失败 → 抛 error_no_captions
```

### 3.2 逐项对比

| 维度 | 捕获生词（失效） | youtube-caption-extractor（有效） | 结论 |
| --- | --- | --- | --- |
| InnerTube 端点 | `www.youtube.com/youtubei/v1/player?key=页面key` | `youtubei.googleapis.com/youtubei/v1/player`（免 key） | ❌ 依赖页面 key |
| 客户端 | 仅 **WEB**（`clientName:'WEB'`, header `1`） | **IOS → ANDROID_VR → MWEB** 多链 | ❌ 单客户端无容错 |
| clientVersion | 取自页面 `ytcfg.INNERTUBE_CLIENT_VERSION`（可能过时） | **硬编码当前版本**（跟随 yt-dlp） | ❌ 依赖页面且易旧 |
| 页面状态依赖 | 强依赖（ytInitialPlayerResponse + ytcfg key） | 仅需 videoID，完全独立 | ❌ 页面一变就失效 |
| 失败容错 | 单 client 失败即整条路径失败 | 多 client 不早退，全败才失败 | ❌ 无容错 |
| 字幕格式 | json3 → srv3 → vtt | 强制 json3 | ✅ 相当 |
| 轨道选择 | 尊重用户选中 + 排除 tlang 翻译轨 + 非 asr 优先（更优） | vssId 语言匹配 | ✅ 捕获生词 更好 |

### 3.3 失效根因（按优先级）

1. **只使用 `WEB` 客户端**——2025–2026 年 YouTube 对 WEB 客户端的 `/player` 响应大规模收紧（未登录/数据中心/风控 IP 常见 `LOGIN_REQUIRED` 或不返回 `captionTracks`）。捕获生词 的**两条路径用的都是 WEB 上下文**（页面快照本质也是 WEB 页面上下文），等于同一堵墙撞两次。
2. **路径 1 依赖 `ytInitialPlayerResponse` 暴露完整 captionTracks**——新版 YouTube 常把字幕信息改为延迟加载 / 不随初始快照下发，快照常拿不到 tracks。
3. **路径 2 依赖页面 `ytcfg.INNERTUBE_API_KEY`**——页面拿不到 key 时直接报 `No INNERTUBE_API_KEY available`；且 `www.youtube.com/youtubei` 域名比 `youtubei.googleapis.com` 更易触发风控。
4. **无多客户端容错 + 无版本维护机制**——clientVersion 过期后没有任何自愈手段。

---

## 4. 修复方案（推荐）

### 4.1 总体思路

**把新包的核心逻辑移植为 `background.js` 的"免 Key 多客户端 InnerTube 直调"，作为 `extractCaptionsText` 的新增第三路径（最强回退）**。已有路径 1/2 保留（对仍生效的页面最快），新路径兜底。

理由：
- 扩展的 **background service worker 发起 fetch 不受页面 CORS 约束**（受 host_permissions 约束），且不依赖页面 cookie/状态；
- 捕获生词 manifest 已有 `<all_urls>`，`youtubei.googleapis.com` 已被覆盖，**无需动 page_inject.js**；
- 改动集中在 `background.js` + `content.js` 两处，约 100 行。

### 4.2 修复后的提取管线

```
extractCaptionsText()
  ├─ 路径1（保留）：页面快照 ytInitialPlayerResponse → tracks → 抓取
  ├─ 路径2（保留）：WEB client InnerTube /player（页面 key）→ tracks → 抓取
  ├─ 路径3（新增）：免Key多客户端直调
  │     background: CC_YT_PLAYER_DIRECT
  │        ├─ ios(20.10.4) → android_vr(1.62.20) → mweb(2.20251209.01.00)
  │        ├─ POST youtubei.googleapis.com/youtubei/v1/player（免key）
  │        └─ 取首个 OK 且含 captionTracks 的响应 → 返回 tracks + 语言
  │     content: chooseDefaultTrack(tracks, selected) → 抓 baseUrl (json3/srv3/vtt)
  └─ 全部失败 → error_no_captions
```

### 4.3 改动清单

#### A. `background.js`（新增 2 个函数 + 1 条消息）

```js
// 1) 客户端指纹（硬编码当前版本，跟随 yt-dlp 维护）
const YT_CLIENT_PROFILES = [
  { name:'ios', clientName:'IOS', clientVersion:'20.10.4', header:'5',
    userAgent:'com.google.ios.youtube/20.10.4 (iPhone16,2; U; CPU iOS 18_3_2 like Mac OS X;)',
    context:{ deviceMake:'Apple', deviceModel:'iPhone16,2', platform:'MOBILE', osName:'iOS', osVersion:'18.3.2.22D82' } },
  { name:'android_vr', clientName:'ANDROID_VR', clientVersion:'1.62.20', header:'28',
    userAgent:'com.google.android.apps.youtube.vr.oculus/1.62.20 (Linux; U; Android 12L; eureka-user Build/SQ3A.220605.009.A1) gzip',
    context:{ deviceMake:'Oculus', deviceModel:'Quest 3', platform:'MOBILE', osName:'Android', osVersion:'12L', androidSdkVersion:32 } },
  { name:'mweb', clientName:'MWEB', clientVersion:'2.20251209.01.00', header:'2',
    userAgent:'Mozilla/5.0 (iPhone; CPU iPhone OS 17_5_1 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.5 Mobile/15E148 Safari/604.1',
    context:{ platform:'MOBILE', osName:'iOS', osVersion:'17.5.1' } },
];

// 2) 免key多客户端直调：返回首个含 captionTracks 的 tracks
async function fetchPlayerDirect(videoId) {
  const failures = [];
  for (const c of YT_CLIENT_PROFILES) {
    try {
      const body = { context:{ client:{ clientName:c.clientName, clientVersion:c.clientVersion, hl:'en', gl:'US', ...c.context }, user:{lockedSafetyMode:false}, request:{useSsl:true} },
                     videoId, contentCheckOk:true, racyCheckOk:true };
      const res = await fetch('https://youtubei.googleapis.com/youtubei/v1/player?prettyPrint=false', {
        method:'POST',
        headers:{ 'Content-Type':'application/json', Accept:'*/*', 'User-Agent':c.userAgent,
                  'X-YouTube-Client-Name':c.header, 'X-YouTube-Client-Version':c.clientVersion,
                  'Origin':'https://www.youtube.com' },
        body: JSON.stringify(body),
      });
      if (!res.ok) { failures.push(`${c.name}: http ${res.status}`); continue; }
      const data = await res.json();
      const st = data?.playabilityStatus?.status;
      if (st && st !== 'OK') { failures.push(`${c.name}: ${st}`); continue; }
      const tracks = data?.captions?.playerCaptionsTracklistRenderer?.captionTracks;
      if (Array.isArray(tracks) && tracks.length) return { tracks, lang: detectLang(tracks) };
      failures.push(`${c.name}: OK but no tracks`);
    } catch (e) { failures.push(`${c.name}: ${e?.message || e}`); }
  }
  return { tracks: [], lang: '', error: failures.join(' | ') };
}

// 3) 消息路由新增分支
if (msg.type === 'CC_YT_PLAYER_DIRECT') {
  try {
    const { tracks, lang } = await fetchPlayerDirect(msg.videoId);
    sendResponse({ ok: tracks.length > 0, tracks, lang });
  } catch (e) { sendResponse({ ok:false, tracks:[], lang:'', error:String(e) }); }
  return true;
}
```

> `detectLang(tracks)` 可复用 捕获生词 思路：优先非 tlang / 非 asr 轨的 `languageCode`。

#### B. `content.js`（`extractCaptionsText` 增加路径 3）

在现有路径 2 的 `catch` 之后、抛错之前插入：

```js
// 3) Fallback: 免Key多客户端 InnerTube 直调（background 执行，不依赖页面状态）
try {
  const { videoId } = getYouTubeVideoInfo();
  if (videoId) {
    const prRes = await chrome.runtime.sendMessage({ type:'CC_YT_PLAYER_DIRECT', videoId });
    const tracks = (prRes && prRes.tracks) || [];
    if (tracks.length) {
      const selected = await getSelectedCaptionTrackMainWorld();
      const pick = chooseDefaultTrack(tracks, selected);
      if (pick && pick.baseUrl) {
        const base = pick.baseUrl;
        const finalLang = pick.languageCode || (prRes && prRes.lang) || 'und';
        for (const fmt of ['json3','srv3','vtt']) {
          try {
            const text = await fetchAndExtract(buildUrlWithFmt(base, fmt));
            if (text && text.trim()) return { text, lang: finalLang };
          } catch {}
        }
      }
    }
  }
} catch (e) { dlog('Direct InnerTube fallback error:', e?.message || e); }
```

#### C. `manifest.json`（可选但建议）

```jsonc
"host_permissions": [
  "https://www.youtube.com/*", "https://*.youtube.com/*",
  "https://*.googlevideo.com/*",
  "https://youtubei.googleapis.com/*",   // 显式声明（<all_urls> 已覆盖，此处为清晰与审核友好）
  "https://generativelanguage.googleapis.com/*",
  "..."
]
```

### 4.4 为什么不需要动 `page_inject.js`

- 新路径在 **background** 执行，不依赖页面主世界，不依赖 `ytcfg` 的 API key / clientVersion / visitorData；
- 字幕 `baseUrl` 的抓取仍复用现有 `fetchCaptionMainWorld`（主世界代理 + 白名单校验），这一层是浏览器 CORS 环境下必需的，保持不变即可。

---

## 5. 备选方案对比

| 方案 | 说明 | 优点 | 缺点 | 结论 |
| --- | --- | --- | --- | --- |
| **A. background 免Key多客户端直调（推荐）** | 移植新包核心到 background | 最可靠、不依赖页面、无 CORS 问题、改动集中 | 需维护 clientVersion | ✅ 首选 |
| B. page_inject 增加免Key多客户端分支 | 在 `CC_YT_API` 分支加多 client | 改动全在一个文件 | 主世界跨域 fetch `youtubei.googleapis.com` 受 **CORS 约束**，可能被拦 | ⚠️ 不推荐为主方案 |
| C. 引入 npm 包 `youtube-caption-extractor` | 走构建流程 | 直接复用经过测试的库 | 捕获生词 无构建系统；包面向 Node/Worker，需适配浏览器 | ❌ 需引入构建，成本高 |
| D. 仅修 WEB client 版本号 | 把页面 clientVersion 换成新版本 | 改动最小 | WEB 客户端本身仍是风控重点，治标不治本 | ❌ 不彻底 |

---

## 6. 长期维护建议

1. **clientVersion 是"保质期"资产**：YouTube 会周期性拒绝旧版本客户端。建议：
   - 把三组 clientVersion/UA 集中定义在 `background.js` 顶部常量区，加注释"跟踪 yt-dlp 近期提交维护"；
   - 可选：设置页提供"客户端版本覆盖"字段，用户/维护者可在不升级插件的情况下手动更新；
   - 可选：做一个脚本（GitHub Actions）定期比对 yt-dlp 的 client version 并提示。
2. **保留路径 1/2 但不依赖**：页面快照 / WEB client 在部分页面仍有效（更快、更省请求），作为首选可降低对备用通道的请求量；失效时自动落到路径 3。
3. **错误日志增强**：路径 3 的 `failures` 数组应通过 `dlog` 输出，便于排障时确认是"哪个 client 失败、什么原因"（`LOGIN_REQUIRED` vs 版本过旧 vs 无 tracks 是三种不同修复方向）。

---

## 7. 验证步骤

1. **加载修复后扩展**：Chrome → `chrome://extensions` → 重新加载；
2. **打开一个有多语言字幕的英文视频**，点击扩展图标打开浮层触发提取；
3. **后台确认**：`chrome://extensions` → 点"Service Worker" → Console，观察：
   - `CC_YT_PLAYER_DIRECT` 是否被调用；
   - 返回的 `tracks` 数量与选中的 client（期望 `ios` 或 `android_vr` 命中）；
4. **边界用例**：
   - 无字幕视频 → 应正确抛 `error_no_captions`（而非因通道失败误报）；
   - 中文/日文字幕视频 → 轨道选择语言正确；
   - 未登录浏览器 / 无痕模式 → 路径 3 应仍可用（免 key 直调不依赖登录）；
   - 断网 → 明确报错而非卡死。
5. **回归**：确认原路径 1/2 仍优先生效（页面快照可用时走快照）。

---

## 8. 附录：关键代码对照

| 能力 | 捕获生词（现有） | youtube-caption-extractor（参考） |
| --- | --- | --- |
| InnerTube 调用 | `page_inject.js` `CC_YT_API`：`www.youtube.com/youtubei/v1/{endpoint}?key=页面key` + `X-Youtube-Client-Name:'1'`(WEB) | `src/index.ts` `fetchPlayerWithClient`：`youtubei.googleapis.com/youtubei/v1/player?prettyPrint=false` 免 key + 多 client |
| 客户端指纹 | 无（固定 WEB） | `CLIENT_PROFILES`（ios/android_vr/mweb，硬编码 UA + clientVersion + context） |
| 失败容错 | 单 client 直接失败 | `fetchPlayer` 遍历 + 不早退 + 首个有 tracks 返回 |
| 版本维护 | 无机制 | 注释明确"跟随 yt-dlp 维护 clientVersion" |
| 字幕解析 | `captionsJsonToText` / `captionsVttToText`（json3/srv3/vtt） | `fetchCaptionTrack`（强制 json3 + he.decode + striptags） |
| 轨道选择 | `chooseDefaultTrack`（尊重用户选择 + 排除 tlang + 非 asr 优先） | `pickCaptionTrack`（vssId `.lang` → `a.lang` → languageCode → 首条） |

---

## 结论

> **捕获生词 字幕提取失效的根因是"只用 WEB 客户端 + 强依赖页面快照/页面 API Key"，这是 2026 年 YouTube 风控最先封禁的通道。**
> **可直接修复：把 `youtube-caption-extractor` 的"免 API Key 多客户端（IOS/ANDROID_VR/MWEB）直调 `youtubei.googleapis.com`"移植到 `background.js` 作为第三回退路径。改动约 100 行（background.js + content.js），不动 page_inject.js，manifest 只需显式补一个域名。修复后对未登录/无痕/风控 IP 都能稳定取回字幕，且原快照路径在可用时仍优先，保持"快 + 稳"兼顾。**
